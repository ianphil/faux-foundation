# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: faux-foundation

services:
  macgyver:
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile
      context: .
  llm-proxy:
    host: containerapp
    language: csharp
    project: ../copilot-llm-svc
    docker:
      path: ./Dockerfile
      context: .
  chat:
    host: containerapp
    language: js
    project: ./apps/chat
    docker:
      path: ./Dockerfile
      context: .
