{
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "githubToken": {
      "value": "${GITHUB_TOKEN}"
    },
    "copilotToken": {
      "value": "${COPILOT_TOKEN}"
    },
    "mindRepo": {
      "value": "${MIND_REPO=ianphil/faux-foundation}"
    },
    "pollInterval": {
      "value": "${POLL_INTERVAL=30s}"
    },
    "copilotResponseTimeoutMs": {
      "value": "${COPILOT_RESPONSE_TIMEOUT_MS=600000}"
    },
    "authClientId": {
      "value": "${AUTH_CLIENT_ID=}"
    },
    "authClientSecret": {
      "value": "${AUTH_CLIENT_SECRET=}"
    },
    "chatCustomDomain": {
      "value": "${CHAT_CUSTOM_DOMAIN=}"
    }
  }
}
